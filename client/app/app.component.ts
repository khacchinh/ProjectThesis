import { Component, OnInit } from '@angular/core';
@Component({
    moduleId: module.id,
    selector: 'app',
    templateUrl: 'app.component.html'
})

export class AppComponent { 
    
    private title = "This is demo Mean2 (MongoDB + Express + NodeJs + Angular2)";

    ngOnInit(){
    }
}